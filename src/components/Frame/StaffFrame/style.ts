import styled from 'styled-components';

export const DeliveryFrameContainer = styled.div`
  position: absolute;
  top: 0;
  left: 0;
  width: calc(100% - 4rem);
  padding: 0 2rem;
`;

export const CookFrameContainer = styled.div``;
