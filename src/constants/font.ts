export enum FontSize {
  XS = '0.625rem',
  S = '0.75rem',
  M = '1rem',
  L = '1.25rem',
  XL = '1.5rem',
  XXL = '2rem',
}

export enum FontWeight {
  BOLD = 700,
  MEDIUM = 500,
  REGULAR = 400,
  LIGHT = 300,
}
